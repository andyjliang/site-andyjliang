export default (state = [], action) => {
  	switch (action.type) {
  		case 'SHOW_POSTS':
    		debugger
      		return state
    	case 'SHOW_ONE_POST':
    		debugger
      		return state
    	default:
      		return state
  	}
}